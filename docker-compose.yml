version: "3"
services:

  usb-pipeline:
    image: usb-pipeline
    volumes: 
        - ./workspace:/workspace
        
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    
    tty: true
